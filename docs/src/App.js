const React = require('react')
const Topbar = require('./Topbar')

class App extends React.Component {
  render() {
    return (
      <div>
        <Topbar />
      </div>
    )
  }
}

module.exports = App
